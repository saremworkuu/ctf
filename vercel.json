{
  "version": 2,
  "builds": [
    {
      "src": "backend/server.ts",
      "use": "@vercel/node"
    },
    {
      "src": "frontend/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ],
  "rewrites": [
    { "source": "/api/(.*)", "destination": "backend/server.ts" },
    { "source": "/image/(.*)", "destination": "backend/server.ts" },
    { "source": "/(.*)", "destination": "frontend/package.json" }
  ]
}
